os: windows
language: csharp
mono: latest

install:
 - choco install dotnetcore-sdk
 - dotnet restore
script:
 - dotnet build ./MintPlayer.UpdateDialog/MintPlayer.UpdateDialog.csproj --configuration Release
